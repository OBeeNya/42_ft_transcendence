{
  "dependencies": {
    "axios": "^1.4.0",
    "js-cookie": "^3.0.5"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  },
  "scripts": {
    "start": "concurrently \"cd back && npm run dev:db:up && sleep 5 && npx prisma migrate dev && npm run start:dev\" \"cd front && npm start\""
  }
}
